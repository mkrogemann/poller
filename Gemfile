source 'https://rubygems.org'

group :test do
  gem 'coveralls', :require => false unless (RUBY_VERSION == '1.8.7' or RUBY_VERSION == '1.9.3')
end

# Specify your gem's dependencies in poller.gemspec
gemspec
